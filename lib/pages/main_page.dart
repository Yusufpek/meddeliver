import 'package:flutter/material.dart';

class MainPaeg extends StatelessWidget {
  const MainPaeg({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
